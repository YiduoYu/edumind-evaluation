server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edumind?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: yuyd25105
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.edumind.entity

  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl